Frontend setup

1. Install deps: `npm install` in frontend folder.
2. Run dev: `npm run dev`.

Notes:
- Login using registered user from backend. Token is stored in `localStorage`.
- Dashboard routes: /dashboard/posted, /dashboard/post, /dashboard/profile, /dashboard/analysis
